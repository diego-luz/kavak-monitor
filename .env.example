# ==================== CONFIGURAÇÕES OBRIGATÓRIAS ====================

# Telegram Bot Token (obtenha com @BotFather no Telegram)
# Formato: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_BOT_TOKEN=

# ==================== CONFIGURAÇÕES OPCIONAIS ====================

# Porta HTTP para acessar a interface web (padrão: 80)
# Altere para outra porta se já tiver outro serviço usando a porta 80
# Exemplo: PORT=5000 para acessar em http://localhost:5000
PORT=5000

# Número máximo de threads para verificação paralela (padrão: 5)
# Recomendado para Raspberry Pi 3: 5
# Pode aumentar para 10-15 em servidores mais potentes
MAX_WORKERS=5

# Quantos sites verificar por ciclo (padrão: 50)
# Com 500 monitoramentos, 50 por ciclo = 10 ciclos = ~100 minutos para rodada completa
BATCH_SIZE=50

# Timeout para requisições HTTP em segundos (padrão: 5)
REQUEST_TIMEOUT=5

# Intervalo entre verificações em minutos (padrão: 10)
# Verificação automática acontece a cada X minutos
CHECK_INTERVAL=10

# Máximo de monitoramentos ativos por IP (padrão: 10)
# Previne abuso e protege recursos do servidor
MAX_MONITORAMENTOS_POR_IP=10

# ==================== INSTRUÇÕES ====================

# 1. Copie este arquivo para .env:
#    cp .env.example .env
#
# 2. Preencha o valor obrigatório (TELEGRAM_BOT_TOKEN)
#
# 3. Ajuste as configurações opcionais se necessário
#
# 4. Inicie o sistema:
#    docker-compose up -d
#
# 5. IMPORTANTE: Nunca commite o arquivo .env no Git!
#    O .gitignore já está configurado para ignorá-lo.

# ==================== RECURSOS ÚTEIS ====================

# Como criar um Telegram Bot:
# Veja o tutorial em: http://seu-ip/guia_telegram.html
#
# Otimização para Raspberry Pi:
# Consulte: otimizacao_raspberry_pi.md
